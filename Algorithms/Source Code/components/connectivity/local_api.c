#include "local_api.h"

esp_err_t local_api_start(void)
{
    /* Local HTTP endpoints expose status, recording control, SD file list,
       download, rename, and delete operations. */
    return ESP_OK;
}
